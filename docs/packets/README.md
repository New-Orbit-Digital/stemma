# Packets
Each file is an executor unit. The planner files it as a GitHub issue: the file body becomes the issue body. The planner triggers it with an `@claude` comment once its dependencies have merged. One unit, one issue, one branch, one PR.
